{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Extension: コールエディタをテスト起動",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/extension",
        "${workspaceFolder}"
      ]
    }
  ]
}

